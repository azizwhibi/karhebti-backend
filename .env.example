# Configuration MongoDB
MONGODB_URI=mongodb://localhost:27017/karhebti

# Configuration JWT
JWT_SECRET=votre-cle-secrete-tres-securisee-changez-moi-en-production

# Configuration serveur
PORT=3000
NODE_ENV=development

# Configuration CORS
FRONTEND_URL=http://localhost:3001

# Configuration Email (SMTP) - OPTIONAL for development
# Choose ONE option below or leave commented to work without email

# Option 1: Gmail (Requires App Password - See EMAIL_SETUP_GUIDE.md)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=randomxcuz@gmail.com
SMTP_PASS=nein lctx bjrv rkcj
SMTP_FROM="Karhebti Support" <noreply@karhebti.com>

# Option 2: Ethereal (Free Testing - See EMAIL_SETUP_GUIDE.md)
# SMTP_HOST=smtp.ethereal.email
# SMTP_PORT=587
# SMTP_USER=your-ethereal-user@ethereal.email
# SMTP_PASS=your-ethereal-password
# SMTP_FROM="Karhebti Support" <noreply@karhebti.com>

# Option 3: No Email (Development) - OTP returned in API response
# Just leave all SMTP variables commented out (current default)

# Note: If email is not configured, OTP codes will be returned in the API response
# and logged to the console for testing purposes.
